{
  "name": "bikasafe-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run build:backend",
    "build:backend": "cd backend && npm install && node scripts/patch-schema.js && npm run build && npx prisma db push",
    "start": "cd backend && npm run start"
  }
}
